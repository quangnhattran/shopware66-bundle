{% sw_extends '@Storefront/storefront/page/content/product-detail.html.twig' %}

{% block page_content %}
    {% if page.product.extensions.bundles.elements|length > 0 %}
        <div class="container">
            <div class="badge bg-success badge-new">
                {{ 'bundle.detail.bundleBadge'|trans }}
            </div>
        </div>
    {% endif %}
    {{ parent() }}

{% endblock %}